# Encoder finalization: reuse the pool fitted by the `trec` run, re-score with the -l encoder
# (hypotheses transfer; only the scoring changes).
run_name: trec_finalize_l
seed: 7
data:
  name: trec
encoder:
  model: dleemiller/finecat-nli-l
pool:
  from_run: trec
